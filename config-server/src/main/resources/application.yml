server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pabloejover/backend-1-config.git
          default-label: main
          search-paths: configuration